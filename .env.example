# Base URL of the backend, WITHOUT a trailing /api — the API client appends
# the /api prefix itself. e.g. https://api.myschoolapp.com  (no trailing slash)
API_BASE_URL=https://test-online-m3hz.onrender.com
